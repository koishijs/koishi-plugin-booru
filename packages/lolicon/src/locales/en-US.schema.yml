$description: Search Settings
endpoint: Lolicon API URL.
r18:
  $description: Whether to fetch R18 images.
  $inner:
    - Non-R18
    - R18 Only
    - Mixed
proxy:
  $description: Pixiv reverse proxy services.
  $inner:
    - i.pixiv.re
    - i.pixiv.cat
    - i.pixiv.nl
    - 
      $description: Custom
      $inner:
        endpoint: URL of reverse proxy server.
excludeAI:
  $description: Whether to allow AI generated content。
  $inner:
    - Exclude AI Works
    - Include AI Works
